# HTTP 流量 (TCP 80)
DST-PORT,80

# HTTPS 流量 (TCP 443)
DST-PORT,443

# DNS 流量 (UDP 53)
DST-PORT,53

# SSH 流量 (TCP 22)
DST-PORT,22

# 邮件协议 (SMTP: TCP 25, IMAP: TCP 143, POP3: TCP 110)
DST-PORT,25
DST-PORT,143
DST-PORT,110

# 常见代理协议 (SOCKS5: TCP/UDP 1080)
DST-PORT,1080

# NTP 时间同步 (UDP 123)
DST-PORT,123

# 常见的文件共享服务端口 (TCP 21, TCP 20 - FTP)
DST-PORT,21
DST-PORT,20